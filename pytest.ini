[pytest]
# Pytest 마커 설정

# 테스트 유형별 마커
markers =
    unit: 단위 테스트 (DB 불필요)
    integration: 통합 테스트 (실제 DB/API 필요)
    slow: 느린 테스트 (> 1분)

# 테스트 경로
pythonpath = .
testpaths = tests

# 설정
asyncio_mode = auto
